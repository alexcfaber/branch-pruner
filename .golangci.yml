version: 2
run:
  timeout: 3m
  tests: true

linters:
  enable:
    - govet
    - staticcheck
    - errcheck
    - ineffassign
    - unparam
    - unused
  disable:
    - gocyclo
    - dupl

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
